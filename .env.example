# HarmoniSync Environment Configuration
# Copy this file to .env and configure with your values
# NEVER commit .env to version control!

# Server Configuration
PORT=8000
HOST=0.0.0.0

# Socket.io Configuration
SOCKET_MAX_BUFFER_SIZE=100000000

# Ngrok Configuration (Get token at ngrok.com)
NGROK_AUTHTOKEN=your_ngrok_token_here

# CORS Configuration (comma-separated origins)

CORS_ORIGINS=*

# Optional: Production Settings
# NODE_ENV=production
# LOG_LEVEL=info

# Optional: Database (if you add one later)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=harmonisync
# DB_USER=your_db_user
# DB_PASSWORD=your_secure_password

# Optional: Authentication (if you add it later)
# JWT_SECRET=your_super_secret_jwt_key_here
# SESSION_SECRET=your_session_secret_here

# Optional: External APIs (if needed)
# API_KEY=your_api_key_here
